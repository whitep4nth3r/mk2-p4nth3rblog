const fetch = require("node-fetch");

module.exports = async function () {
  // const youtube = await fetch(`${process.env.DOMAIN}/api/youtube`);
  // const youtubeData = await youtube.json();

  // const twitch = await fetch(`${process.env.DOMAIN}/api/twitch`);
  // const twitchData = await twitch.json();

  // const github = await fetch(`${process.env.DOMAIN}/api/github`);
  // const githubData = await github.json();

  // const twitter = await fetch(`${process.env.DOMAIN}/api/twitter`);
  // const twitterData = await twitter.json();

  // const staticDashboard = {
  //   github: githubData,
  //   twitch: twitchData,
  //   twitter: twitterData,
  //   youtube: youtubeData,
  // };

  return {
    // staticDashboard,
  };
};
